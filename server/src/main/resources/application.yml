server:
  port: ${SERVER_PORT:8080}
  address: 0.0.0.0

spring:
  application:
    name: property-server
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/property_management}
  jackson:
    serialization:
      write-dates-as-timestamps: false

openclaw:
  base-url: ${OPENCLAW_BASE_URL:https://openclaw.example.com}

admin:
  api-key: ${ADMIN_API_KEY:dev-admin-123456}
  session-ttl-minutes: ${ADMIN_SESSION_TTL_MINUTES:720}

management:
  endpoints:
    web:
      exposure:
        include: health,info
